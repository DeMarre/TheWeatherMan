// const express = require("express");
// const app = express();

// app.set("view engine", "pug");
// app.set("views", "./views");

// // use body parser tp parse forms and JSON
// app.use(bodyParser.urlencoded({ extended: false }));
// app.use(bodyParser.json());

// app.get("/", (req, res) => {
//   res.render("form");
// });

// app.post("/guestbook", (req, res) => {
//   const formData = {
//     name: req.body.name,
//     message: req.body.message,
//   };
//   res.render("formsubmitted", formData);
// });

// app.listen(3000);
